{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Docker Remote Debug",
      "address": "localhost",
      "port": 9229,
      "remoteRoot": "/app",
      "localRoot": "${workspaceFolder}",
      "preLaunchTask": "Docker Build and Run"
    }
  ]
}
